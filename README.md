# ShieleQuiz (web)

A small Flask version of the Genlayer community quiz. Questions live in `questions.py` — no database.

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5050
